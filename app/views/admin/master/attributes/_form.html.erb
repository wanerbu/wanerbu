  <%= semantic_form_for([:admin, :master, @attribute], :html => { :class => 'form-horizontal' }) do |f| %>
      <%= f.input :name %>
      <%= f.input :unit %>
      <%= f.input :intro %>
    <%= f.actions do %>
      <%= f.action :submit, :button_html => { :class => "btn btn-primary" }, :as => :button %>
      <%= f.action :cancel, :as => :link %>
    <% end -%>
  <% end %>
