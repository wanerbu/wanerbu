<%= link_to t('views.defaults.actions.show'), user_master_court_path(target)%>
 | 
<%= link_to t('views.defaults.actions.edit'), edit_user_master_court_path(target)%>
 | 
<%= link_to t('views.defaults.actions.destroy'), user_master_court_path(target), 
                { method: :delete, confirm: t('activemodel.confirm.destroy') } %>
