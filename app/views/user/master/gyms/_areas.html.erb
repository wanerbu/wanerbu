<% if !areas.blank? %>
  <% if @gym == nil %>
    <%= collection_select(:gym, :area_id, areas.all, :id, :area) %>
  <% else %>
    <%= collection_select(:gym, :area_id, areas.all, :id, :area,{:selected => @gym.area_id}) %>
  <% end %>
<% else %>
<% end %>

