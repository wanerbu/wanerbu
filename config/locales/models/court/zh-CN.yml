zh-CN:
  activerecord:
    models:
      court: 场地
    attributes:
      court: &court
        min_unit: "最小预订单位"
        reservation_type: "预订类型"
    errors:
      models:
        court:
          attributes:
  datagrid:
    court_report:
      columns:
        <<: *court
