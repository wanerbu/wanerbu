zh-CN:
  activerecord:
    models:
      property: 属性
    attributes:
      property: &property
        name: "属性名"
        unit: "属性单位"
        intro: "属性说明"
    errors:
      models:
        property:
          attributes:
  datagrid:
    property_report:
      columns:
        <<: *property
